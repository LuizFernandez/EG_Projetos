digraph {
	"int z = 0"
	inicio
	inicio -> "int z = 0"
	"while (z <= 40)"
	"int z = 0"
	"int z = 0" -> "while (z <= 40)"
	"while (z <= 40)"
	"while (z <= 40)" -> "if (z >= 5)" [label=true]
	"if (z >= 5)"
	"if (z >= 5)" -> "z = z + 5" [label=true]
	fim
	"z = z + 5"
	"z = z + 5" -> fim
	"if (z >= 5)"
	"if (z >= 5)" -> "z = z + 1" [label=false]
	fim
	"z = z + 1"
	"z = z + 1" -> fim
	"while (z <= 40)" [shape=diamond]
	"if (z >= 5)" [shape=diamond]
	"while (z <= 40)" [shape=diamond]
	"if (z >= 5)" [shape=diamond]
}
